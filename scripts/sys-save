#!/bin/bash
MSG="${1:-Auto-save $(date +'%Y-%m-%d %H:%M')}"
echo ">> Snapshotting User Configs..."
chezmoi re-add
cd ~/.local/share/chezmoi
git add .
git commit -m "$MSG"
git push origin main
echo ">> Pushed to GitHub."
